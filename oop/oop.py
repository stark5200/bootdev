# main function goes here
# some git commands:
# git push --force to force changes
# git status --short to get short message about current state
# git commit -a to commit changes and skipping staging
#
def oop():
  max = 10
  return max